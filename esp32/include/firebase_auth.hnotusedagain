#pragma once

#define ENABLE_USER_AUTH

#include <FirebaseClient.h>
#include <WiFiClientSecure.h>

// Global Firebase app/auth/session
extern FirebaseApp app;
extern UserAuth user_auth;
extern WiFiClientSecure ssl_client;

// Auth helpers
void initFirebase();
